services:
    laravel.test:
        container_name: 'Staging'
        build:
            context: .
            dockerfile: staging/Dockerfile
        expose:
            - '3000'
        volumes:
            - 'storage_volume:/var/www/html/storage'
